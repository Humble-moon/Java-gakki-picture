# 本地开发环境配置
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gakki_picture_dev?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: dev_password
  redis:
    host: localhost
    port: 6379
    password:
server:
  port: 8080
  servlet:
    context-path:
logging:
  level:
    com.gakki: DEBUG
    org.springframework: DEBUG